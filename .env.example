# Base URL of the Nomad backend API.
# Copy this file to `.env` and set the value to your backend.
# If unset (or the backend is unreachable), the app falls back to the
# bundled mock data in src/api/mockData.ts, so the UI always renders.
VITE_API_BASE_URL=http://localhost:4000/api
